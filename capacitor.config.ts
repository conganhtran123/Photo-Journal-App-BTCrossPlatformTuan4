import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.photojournal',
  appName: 'PhotoJournal',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
